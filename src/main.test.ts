import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
//import { config } from "./main";

describe("Game", () => {
  beforeEach(() => {});

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it("creates", () => {
    //const g = config;
    expect(true);
  });
});
